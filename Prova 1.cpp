#include <stdio.h>
#include <math.h>

  int main (){
  char op;
  
 printf ("escolha qual opcao deseja: ");
 scanf("%c", &op);

switch(letra){
	
case 'a': printf("
	
   printf("Digite dois valores: ");
  scanf("%i,%i", &a, &b);	
  
  x = pow(a, 2) + pow(b, 2);
  hip =sqrt(x);
  seno = a / hip;
  printf(" A hipotenusa vale %.3i e o seno vale %.3i", hip, seno);
  "); break;
case 'b': printf(" "); break;
case 'c': printf(" "); break;
default: printf("opcao invalida");
}

}
